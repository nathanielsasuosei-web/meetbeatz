node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.*
!.env.example
# Runtime upload folder on disk (kept only so files written by older versions
# can be imported into the database — see src/lib/files.ts). Anchored to the
# repository root on purpose: a bare `uploads/` also matches source paths like
# src/app/api/admin/uploads/, which silently kept those routes out of git.
/uploads/
public/meetbeatz-source.zip
.DS_Store
npm-debug.log*
.vercel
.pgdata/
.pgpwd
.postgres-data/
seed-settings.js
